To use this example, you must first install php as below.

sudo apt-get install php5

To use sqlite3 with PHP you must install the PHP sqlite3 libraries:

sudo apt-get install php5-sqlite

To run the PHP server type:

make run

To execute the code, you must open Firefox and go to web address below:

http://localhost:8000/initializeDB.php

